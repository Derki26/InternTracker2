import SwiftUI

struct ApprovalsView: View {
    var body: some View {
        List {
            Text("Approvals (placeholder)")
                .foregroundStyle(.secondary)
        }
        .navigationTitle("Approve Logs")
    }
}
